<%= simple_form_for group,
    data: {
      caracters_and_groups_target: "form",
      action: "submit->caracters_and_groups#send"
      } do |f| %>
  <%= f.button :submit, class:'btn btn-dark' %>
<% end %>
